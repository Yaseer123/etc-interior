# Prisma / Supabase connection strings
# Replace <USER>:<PASSWORD>@<HOST>:<PORT>/<DB> with your Supabase details

# Primary connection string used by Prisma Client
DATABASE_URL="postgresql://<USER>:<PASSWORD>@<HOST>:5432/<DB>?schema=public"

# Optional: direct connection string (useful for migrations)
DIRECT_URL="postgresql://<USER>:<PASSWORD>@<HOST>:5432/<DB>?schema=public"

# Optional: Supabase client (if you use supabase-js on the frontend)
# NEXT_PUBLIC_SUPABASE_URL="https://<your-project-ref>.supabase.co"
# NEXT_PUBLIC_SUPABASE_ANON_KEY="<your-anon-key>